import light from 'themes/light'

import type { Colors } from '../../types/colors'

export const colors = {
  'Hint_color--error': light.errorColor,
  'Hint-color--success': light.successColor,
} satisfies Colors

export default colors
