import dark from 'themes/dark'

import type { Colors } from '../../types/colors'

export const colors = {
  'Hint_color--error': dark.errorColor,
  'Hint-color--success': dark.successColor,
} satisfies Colors

export default colors
